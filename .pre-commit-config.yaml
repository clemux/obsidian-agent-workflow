default_install_hook_types: [pre-push]

repos:
  - repo: local
    hooks:
      - id: publication-boundary
        name: Public-safe repository boundary
        entry: uv run python scripts/check_publication_boundary.py
        language: system
        pass_filenames: false
        stages: [pre-push]
