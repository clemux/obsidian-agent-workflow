interface:
  display_name: "OAW"
  short_description: "Resolve Obsidian IDs and manage task/session workflow."
  default_prompt: "Use $oaw to resolve an obs:<ID>, manage its lifecycle, and synchronize the owning agent session."
